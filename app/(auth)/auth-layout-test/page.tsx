// app/(auth)/test-auth-layout/page.tsx
export default function TestAuthLayoutPage() {
  return (
    <div>
      <h1>Test Page for Auth Layout</h1>
      <p>If this page has no main sidebar or header, the (auth) route group layout is working.</p>
      <p>The background should be a blue/indigo gradient and this text should be centered.</p>
    </div>
  );
}
